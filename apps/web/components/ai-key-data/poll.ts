// [AI Key Data] 폴링 주기 — 이 페이지 카드 **전부**가 이 한 값을 쓴다.
//
// ★2026-08-31 사용자 지시로 1시간 고정. 그 전에는 카드마다 5분·10분·30분으로 갈려 있었는데,
//   원천이 전부 **하루 한 번**(아침 `ai_key_data.bat` 한 방)이라 그보다 자주 묻는 건 전부 낭비다.
//   수기 4종(GPU임대지수·금리\·Ramp·macro_releases)도 사람이 하루 한 번 이하로 갱신한다.
// ★한 곳에 두는 이유: 갈라 두면 "이 카드만 왜 안 바뀌지" 를 카드 6개에서 찾게 된다.
//   주기를 바꿔야 하면 여기 한 줄만 고친다.
export const POLL_MS = 3_600_000; // 1h
